import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="text-center py-5 bg-blue-500 text-white">
            <h1 className="text-4xl font-bold">Travel once, sign in anywhere</h1>
        </header>
    );
}

export default Header;